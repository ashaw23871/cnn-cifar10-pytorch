# cnn-cifar10-pytorch
A Convolutional Neural Network (CNN) built with PyTorch to classify CIFAR-10 images. The project covers data preprocessing, model architecture, training, and evaluation with test accuracy measurement.
